name: Run compatibility tests
on:
  workflow_dispatch:
  workflow_call:

concurrency:
  group: compatibility-tests
  cancel-in-progress: false

defaults:
  run:
    shell: bash

jobs:
  tests-compatibility-generate:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ 'ubuntu-latest' ]
        target: [ 'jvmAll', 'js', 'native' ]
        include:
          - os: 'macos-14'
            target: 'macos'
          - os: 'macos-14'
            target: 'ios'
          - os: 'macos-14'
            target: 'watchos'
          - os: 'macos-14'
            target: 'tvos'
          - os: 'windows-latest'
            target: 'native'
          - os: 'ubuntu-latest'
            target: 'connectedAndroid'
            android-api-level: 21
          - os: 'ubuntu-latest'
            target: 'connectedAndroid'
            android-api-level: 27
          - os: 'ubuntu-latest'
            target: 'connectedAndroid'
            android-api-level: 33
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-gradle

      - if: matrix.os == 'windows-latest'
        uses: msys2/setup-msys2@v2
        with:
          release: false
          install: mingw-w64-x86_64-openssl

      - name: Run tests
        if: ${{ matrix.target != 'connectedAndroid' }}
        run: ./gradlew ${{ matrix.target }}ProviderTest -Pckbuild.providerTests.step=compatibility.generate -Pckbuild.testtool.instanceId=${{ matrix.os }}-${{ matrix.target }}-${{ matrix.android-api-level }} --continue

      - name: Run tests (android)
        if: ${{ matrix.target == 'connectedAndroid' }}
        uses: ./.github/actions/run-android-emulator-tests
        with:
          api-level: ${{ matrix.android-api-level }}
          test-reports-classifier: 'compatibility-generate'
          run: ./gradlew ${{ matrix.target }}ProviderTest -Pckbuild.providerTests.step=compatibility.generate -Pckbuild.testtool.instanceId=${{ matrix.os }}-${{ matrix.target }}-${{ matrix.android-api-level }} --continue

      - name: Dump testtool server-storage
        run: ./gradlew dumpTesttoolServerStorage -Pckbuild.testtool.instanceId=${{ matrix.os }}-${{ matrix.target }}-${{ matrix.android-api-level }}
      - name: Upload testtool server-storage dump
        uses: actions/upload-artifact@v4
        with:
          name: compatibility-tests-data-${{ matrix.os }}-${{ matrix.target }}-${{ matrix.android-api-level }}
          path: build/testtool/server-storage-dump
          if-no-files-found: error

  tests-compatibility-validate:
    needs: [ tests-compatibility-generate ]
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ 'ubuntu-latest' ]
        target: [ 'jvmAll', 'js', 'native' ]
        include:
          - os: 'macos-14'
            target: 'macos'
          - os: 'macos-14'
            target: 'ios'
          - os: 'macos-14'
            target: 'watchos'
          - os: 'macos-14'
            target: 'tvos'
          - os: 'windows-latest'
            target: 'native'
          - os: 'ubuntu-latest'
            target: 'connectedAndroid'
            android-api-level: 21
          - os: 'ubuntu-latest'
            target: 'connectedAndroid'
            android-api-level: 27
          - os: 'ubuntu-latest'
            target: 'connectedAndroid'
            android-api-level: 33
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-gradle

      - if: matrix.os == 'windows-latest'
        uses: msys2/setup-msys2@v2
        with:
          release: false
          install: mingw-w64-x86_64-openssl

      - name: Download testtool server-storage dump
        uses: actions/download-artifact@v4
        with:
          pattern: compatibility-tests-data-*
          merge-multiple: true
          path: build/testtool/server-storage-dump

      - name: Restore testtool server-storage
        run: ./gradlew restoreTesttoolServerStorage --no-daemon

      - name: Run tests
        if: ${{ matrix.target != 'connectedAndroid' }}
        run: ./gradlew ${{ matrix.target }}ProviderTest -Pckbuild.providerTests.step=compatibility.validate -Pckbuild.testtool.instanceId=${{ matrix.os }}-${{ matrix.target }}-${{ matrix.android-api-level }} --continue

      - name: Run tests (android)
        if: ${{ matrix.target == 'connectedAndroid' }}
        uses: ./.github/actions/run-android-emulator-tests
        with:
          api-level: ${{ matrix.android-api-level }}
          test-reports-classifier: 'compatibility-validate'
          run: ./gradlew ${{ matrix.target }}ProviderTest -Pckbuild.providerTests.step=compatibility.validate -Pckbuild.testtool.instanceId=${{ matrix.os }}-${{ matrix.target }}-${{ matrix.android-api-level }} --continue
