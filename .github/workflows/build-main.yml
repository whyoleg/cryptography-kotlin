name: Build main
on:
  workflow_dispatch:
  push:
    paths-ignore:
      - samples/**
    branches:
      - main

jobs:
  checks:
    uses: ./.github/workflows/run-checks.yml

  publish:
    needs: [ checks ]
    uses: ./.github/workflows/publish-snapshot.yml
    secrets: inherit
